CREATE TABLE todos (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT,
    content TEXT,
    is_public INTEGER,
    food_orange INTEGER,
    food_apple INTEGER,
    food_banana INTEGER,
    pub_date TEXT,
    qty1 INTEGER,
    qty2 INTEGER,
    qty3 INTEGER
);
